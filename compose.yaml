services:
  mariadb:
    image: mariadb:11.8.2
    container_name: mariadb_bdnovaAPI
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: bdnova_api
      MYSQL_USER: bdnova
      MYSQL_PASSWORD: bdnova
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
    restart: unless-stopped

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: phpmyadmin_bdnovaAPI
    environment:
      PMA_HOST: mariadb
      PMA_USER: root
      PMA_PASSWORD: root
    ports:
      - "8081:80"
    depends_on:
      - mariadb
    restart: unless-stopped

volumes:
  db_data:
